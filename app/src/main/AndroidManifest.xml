<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.loviedo.planwes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name="net.loviedo.planwes.activities.TensParameterActivity"
            android:theme="@android:style/Theme.Material.Light.Dialog.NoActionBar"></activity>
        <activity
            android:name="net.loviedo.planwes.activities.TensActivity"
            android:parentActivityName="net.loviedo.planwes.MainActivity" />
        <activity android:name="net.loviedo.planwes.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>